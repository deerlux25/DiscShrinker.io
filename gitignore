# dependencies
node_modules/
server/node_modules/

# build output
dist/

# user-uploaded / temp video files — never commit real uploads
uploads/*
!uploads/.gitkeep

# server-generated runtime state — machine-specific, not source
server/ticket-counter.json
server/incidents.json
server/stats.json

# env / secrets
.env
.env.*

# editor / OS
.vscode/
.DS_Store
